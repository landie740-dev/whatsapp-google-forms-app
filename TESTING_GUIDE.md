# 🧪 Testing & Integration Guide\n\nComplete guide to test your WhatsApp-Google Forms integration.\n\n## Table of Contents\n1. [Local Testing](#local-testing)\n2. [Unit Tests](#unit-tests)\n3. [Integration Tests](#integration-tests)\n4. [End-to-End Testing](#end-to-end-testing)\n5. [Debugging](#debugging)\n6. [Monitoring](#monitoring)\n\n---\n\n## Local Testing\n\n### Start the App\n\n```bash\nnpm run dev\n```\n\nYou should see:\n```\nServer running on port 3000\nEnvironment: development\n```\n\n### Test Health Endpoint\n\n**Using curl:**\n```bash\ncurl http://localhost:3000/health\n```\n\n**Expected response:**\n```json\n{\n  \"status\": \"ok\",\n  \"timestamp\": \"2026-06-02T20:55:00.000Z\",\n  \"environment\": \"development\"\n}\n```\n\n### Test Form Webhook\n\n**Using curl:**\n```bash\ncurl -X POST http://localhost:3000/api/forms/webhook \\\n  -H \"Content-Type: application/json\" \\\n  -d '{\n    \"formId\": \"test_form_123\",\n    \"formTitle\": \"Prayer Request Form\",\n    \"respondentEmail\": \"test@example.com\",\n    \"responses\": {\n      \"Name\": \"John Doe\",\n      \"Prayer Request\": \"Please pray for my family\",\n      \"Urgency\": \"High\"\n    }\n  }'\n```\n\n**Expected response:**\n```json\n{\n  \"success\": true,\n  \"message\": \"Notifications sent successfully\",\n  \"responseId\": \"response_1234567890\",\n  \"messageIds\": [\"msg_1\", \"msg_2\"],\n  \"workersNotified\": 2\n}\n```\n\n### Test Worker Response\n\n```bash\ncurl -X POST http://localhost:3000/api/webhooks/worker-response \\\n  -H \"Content-Type: application/json\" \\\n  -d '{\n    \"workerPhone\": \"+1234567890\",\n    \"message\": \"I will handle this\",\n    \"assignmentId\": \"assign_123\"\n  }'\n```\n\n### Using Postman\n\n1. Download [Postman](https://www.postman.com/downloads/)\n2. Import this collection:\n\n```json\n{\n  \"info\": {\n    \"name\": \"Church Notifier API\",\n    \"schema\": \"https://schema.getpostman.com/json/collection/v2.1.0/collection.json\"\n  },\n  \"item\": [\n    {\n      \"name\": \"Health Check\",\n      \"request\": {\n        \"method\": \"GET\",\n        \"url\": \"http://localhost:3000/health\"\n      }\n    },\n    {\n      \"name\": \"Test Form Webhook\",\n      \"request\": {\n        \"method\": \"POST\",\n        \"url\": \"http://localhost:3000/api/forms/webhook\",\n        \"body\": {\n          \"mode\": \"raw\",\n          \"raw\": \"{\\n  \\\"formId\\\": \\\"test_form\\\",\\n  \\\"formTitle\\\": \\\"Prayer Request\\\",\\n  \\\"respondentEmail\\\": \\\"test@example.com\\\",\\n  \\\"responses\\\": {\\n    \\\"Name\\\": \\\"John Doe\\\",\\n    \\\"Prayer Request\\\": \\\"Please pray\\\"\\n  }\\n}\"\n        }\n      }\n    },\n    {\n      \"name\": \"Test Worker Response\",\n      \"request\": {\n        \"method\": \"POST\",\n        \"url\": \"http://localhost:3000/api/webhooks/worker-response\",\n        \"body\": {\n          \"mode\": \"raw\",\n          \"raw\": \"{\\n  \\\"workerPhone\\\": \\\"+1234567890\\\",\\n  \\\"message\\\": \\\"I will handle this\\\",\\n  \\\"assignmentId\\\": \\\"assign_123\\\"\\n}\"\n        }\n      }\n    }\n  ]\n}\n```\n\n---\n\n## Unit Tests\n\n### Setup Jest\n\n```bash\nnpm install --save-dev jest @types/jest ts-jest\n```\n\nCreate `jest.config.js`:\n\n```javascript\nmodule.exports = {\n  preset: 'ts-jest',\n  testEnvironment: 'node',\n  roots: ['<rootDir>/src'],\n  testMatch: ['**/__tests__/**/*.test.ts'],\n  moduleFileExtensions: ['ts', 'js', 'json']\n};\n```\n\n### Test Google Forms Service\n\nCreate `src/services/__tests__/googleForms.test.ts`:\n\n```typescript\nimport googleFormsService from '../googleForms';\n\ndescribe('GoogleFormsService', () => {\n  it('should categorize prayer requests', () => {\n    const response = {\n      'Prayer Request': 'Please pray for healing'\n    };\n\n    const category = (googleFormsService as any).categorizeResponse(response);\n    expect(category).toBe('prayer');\n  });\n\n  it('should categorize counseling requests', () => {\n    const response = {\n      'Issue': 'I need counseling advice'\n    };\n\n    const category = (googleFormsService as any).categorizeResponse(response);\n    expect(category).toBe('counseling');\n  });\n\n  it('should calculate high priority for urgent requests', () => {\n    const response = {\n      'Message': 'URGENT: Emergency prayer needed'\n    };\n\n    const priority = (googleFormsService as any).calculatePriority(response);\n    expect(priority).toBe('high');\n  });\n\n  it('should calculate medium priority for important requests', () => {\n    const response = {\n      'Message': 'This is important and should be handled soon'\n    };\n\n    const priority = (googleFormsService as any).calculatePriority(response);\n    expect(priority).toBe('medium');\n  });\n\n  it('should calculate low priority for normal requests', () => {\n    const response = {\n      'Message': 'Just a regular request'\n    };\n\n    const priority = (googleFormsService as any).calculatePriority(response);\n    expect(priority).toBe('low');\n  });\n\n  it('should process webhook response', () => {\n    const data = {\n      formId: 'form_123',\n      formTitle: 'Prayer Request Form',\n      respondentEmail: 'test@example.com',\n      responses: {\n        'Name': 'John',\n        'Prayer Request': 'Please pray urgently'\n      }\n    };\n\n    const formResponse = googleFormsService.processWebhookResponse(data);\n    \n    expect(formResponse.formId).toBe('form_123');\n    expect(formResponse.category).toBe('prayer');\n    expect(formResponse.priority).toBe('high');\n  });\n});\n```\n\n### Test Worker Router Service\n\nCreate `src/services/__tests__/workerRouter.test.ts`:\n\n```typescript\nimport workerRouterService from '../workerRouter';\nimport { ChurchWorker } from '../../models/ChurchWorker';\nimport { FormResponse } from '../../models/FormResponse';\n\ndescribe('WorkerRouterService', () => {\n  beforeEach(() => {\n    // Reset state\n    (workerRouterService as any).workers.clear();\n  });\n\n  it('should register a worker', () => {\n    const worker: ChurchWorker = {\n      id: 'worker_1',\n      name: 'John Doe',\n      whatsappNumber: '+1234567890',\n      role: 'Pastor',\n      department: 'pastoral',\n      specializations: ['prayer'],\n      isAvailable: true,\n      assignedCategories: ['prayer', 'counseling'],\n      maxAssignments: 5,\n      currentAssignments: 0,\n      createdAt: new Date(),\n      updatedAt: new Date()\n    };\n\n    workerRouterService.registerWorker(worker);\n    expect((workerRouterService as any).workers.size).toBe(1);\n  });\n\n  it('should get available workers', () => {\n    const worker: ChurchWorker = {\n      id: 'worker_1',\n      name: 'John Doe',\n      whatsappNumber: '+1234567890',\n      role: 'Pastor',\n      department: 'pastoral',\n      specializations: ['prayer'],\n      isAvailable: true,\n      assignedCategories: ['prayer'],\n      maxAssignments: 5,\n      currentAssignments: 0,\n      createdAt: new Date(),\n      updatedAt: new Date()\n    };\n\n    workerRouterService.registerWorker(worker);\n    const available = workerRouterService.getAvailableWorkers();\n    \n    expect(available.length).toBe(1);\n    expect(available[0].id).toBe('worker_1');\n  });\n\n  it('should not include unavailable workers', () => {\n    const worker: ChurchWorker = {\n      id: 'worker_1',\n      name: 'John Doe',\n      whatsappNumber: '+1234567890',\n      role: 'Pastor',\n      department: 'pastoral',\n      specializations: ['prayer'],\n      isAvailable: false,\n      assignedCategories: ['prayer'],\n      maxAssignments: 5,\n      currentAssignments: 0,\n      createdAt: new Date(),\n      updatedAt: new Date()\n    };\n\n    workerRouterService.registerWorker(worker);\n    const available = workerRouterService.getAvailableWorkers();\n    \n    expect(available.length).toBe(0);\n  });\n\n  it('should route response to matching category worker', () => {\n    const worker: ChurchWorker = {\n      id: 'worker_1',\n      name: 'Prayer Team Lead',\n      whatsappNumber: '+1234567890',\n      role: 'Leader',\n      department: 'prayer',\n      specializations: ['prayer'],\n      isAvailable: true,\n      assignedCategories: ['prayer'],\n      maxAssignments: 5,\n      currentAssignments: 0,\n      createdAt: new Date(),\n      updatedAt: new Date()\n    };\n\n    workerRouterService.registerWorker(worker);\n\n    const formResponse: FormResponse = {\n      id: 'response_1',\n      timestamp: new Date(),\n      formId: 'form_1',\n      formTitle: 'Prayer Form',\n      respondentEmail: 'test@example.com',\n      responses: { 'Prayer': 'Please pray' },\n      category: 'prayer',\n      priority: 'low'\n    };\n\n    const routed = workerRouterService.routeResponse(formResponse);\n    expect(routed.length).toBe(1);\n    expect(routed[0].id).toBe('worker_1');\n  });\n});\n```\n\n### Test WhatsApp Service\n\nCreate `src/services/__tests__/whatsapp.test.ts`:\n\n```typescript\nimport whatsappService from '../whatsapp';\n\ndescribe('WhatsAppService', () => {\n  it('should build prayer message', () => {\n    const formResponse = {\n      category: 'prayer',\n      priority: 'low',\n      responses: {\n        'Prayer Request': 'Please pray for my family'\n      },\n      respondentEmail: 'test@example.com'\n    };\n\n    const message = (whatsappService as any).buildNotificationMessage(formResponse);\n    \n    expect(message).toContain('Prayer Request');\n    expect(message).toContain('test@example.com');\n    expect(message).toContain('Please pray for my family');\n  });\n\n  it('should build urgent message for high priority', () => {\n    const formResponse = {\n      category: 'counseling',\n      priority: 'high',\n      responses: {\n        'Issue': 'Emergency counseling needed'\n      },\n      respondentEmail: 'test@example.com'\n    };\n\n    const message = (whatsappService as any).buildNotificationMessage(formResponse);\n    \n    expect(message).toContain('URGENT');\n    expect(message).toContain('🚨');\n  });\n});\n```\n\n### Run Tests\n\n```bash\nnpm test\n\n# Watch mode\nnpm test -- --watch\n\n# With coverage\nnpm test -- --coverage\n```\n\n---\n\n## Integration Tests\n\n### Test Full Workflow\n\nCreate `src/__tests__/integration.test.ts`:\n\n```typescript\nimport request from 'supertest';\nimport app from '../index';\nimport workerRouterService from '../services/workerRouter';\nimport { ChurchWorker } from '../models/ChurchWorker';\n\ndescribe('Integration Tests', () => {\n  beforeAll(() => {\n    // Register test workers\n    const worker: ChurchWorker = {\n      id: 'test_worker_1',\n      name: 'Test Worker',\n      whatsappNumber: '+1234567890',\n      role: 'Tester',\n      department: 'prayer',\n      specializations: ['prayer'],\n      isAvailable: true,\n      assignedCategories: ['prayer', 'counseling'],\n      maxAssignments: 5,\n      currentAssignments: 0,\n      createdAt: new Date(),\n      updatedAt: new Date()\n    };\n\n    workerRouterService.registerWorker(worker);\n  });\n\n  it('should handle complete workflow: form -> route -> notify', async () => {\n    const response = await request(app)\n      .post('/api/forms/webhook')\n      .send({\n        formId: 'test_form',\n        formTitle: 'Test Form',\n        respondentEmail: 'test@example.com',\n        responses: {\n          'Name': 'Test User',\n          'Prayer Request': 'Please pray for my healing'\n        }\n      })\n      .expect(200);\n\n    expect(response.body.success).toBe(true);\n    expect(response.body.workersNotified).toBeGreaterThan(0);\n  });\n\n  it('should return 202 when no workers available', async () => {\n    // Unregister all workers\n    (workerRouterService as any).workers.clear();\n\n    const response = await request(app)\n      .post('/api/forms/webhook')\n      .send({\n        formId: 'test_form',\n        formTitle: 'Test Form',\n        respondentEmail: 'test@example.com',\n        responses: {\n          'Prayer Request': 'Please pray'\n        }\n      })\n      .expect(202);\n\n    expect(response.body.success).toBe(false);\n  });\n});\n```\n\n```bash\nnpm install --save-dev supertest @types/supertest\nnpm test -- integration.test.ts\n```\n\n---\n\n## End-to-End Testing\n\n### Real Google Forms Test\n\n1. Create a test Google Form\n2. Connect it to a Google Sheet\n3. Get the spreadsheet ID\n4. Set up webhook in Google Apps Script:\n\n```javascript\nfunction onFormSubmit(e) {\n  const payload = {\n    formId: 'your_form_id',\n    formTitle: e.source.getTitle(),\n    respondentEmail: e.response.getRespondentEmail(),\n    responses: {}\n  };\n\n  const itemResponses = e.response.getItemResponses();\n  itemResponses.forEach(response => {\n    payload.responses[response.getItem().getTitle()] = response.getResponse();\n  });\n\n  const options = {\n    method: 'post',\n    payload: JSON.stringify(payload),\n    contentType: 'application/json'\n  };\n\n  UrlFetchApp.fetch('https://your-app.herokuapp.com/api/forms/webhook', options);\n}\n```\n\n### Real Twilio Test\n\nAdd to `.env`:\n```\nTWILIO_TEST_PHONE=+1234567890\n```\n\nCreate test:\n\n```typescript\nit('should send real WhatsApp message', async () => {\n  const messageId = await whatsappService.sendMessage({\n    to: process.env.TWILIO_TEST_PHONE || '',\n    body: '🧪 Test message from church notifier'\n  });\n\n  expect(messageId).toBeDefined();\n  expect(messageId.length).toBeGreaterThan(0);\n});\n```\n\n---\n\n## Debugging\n\n### Enable Debug Logs\n\nSet in `.env`:\n```\nLOG_LEVEL=debug\n```\n\n### VS Code Debugger\n\nCreate `.vscode/launch.json`:\n\n```json\n{\n  \"version\": \"0.2.0\",\n  \"configurations\": [\n    {\n      \"type\": \"node\",\n      \"request\": \"launch\",\n      \"name\": \"Launch Program\",\n      \"skipFiles\": [\"<node_internals>/**\"],\n      \"program\": \"${workspaceFolder}/dist/index.js\",\n      \"preLaunchTask\": \"tsc: build\",\n      \"outFiles\": [\"${workspaceFolder}/dist/**/*.js\"]\n    }\n  ]\n}\n```\n\nPress F5 to start debugging.\n\n### Check Logs\n\n```bash\n# View error logs\ntail -f error.log\n\n# View all logs\ntail -f combined.log\n```\n\n---\n\n## Monitoring\n\n### Setup UptimeRobot\n\n1. Go to https://uptimerobot.com\n2. Add monitor:\n   - URL: `https://your-app/health`\n   - Check interval: 5 minutes\n3. Set alert email\n\n### Setup Error Tracking with Sentry\n\n```bash\nnpm install @sentry/node\n```\n\nAdd to `src/index.ts`:\n\n```typescript\nimport * as Sentry from '@sentry/node';\n\nSentry.init({\n  dsn: process.env.SENTRY_DSN,\n  environment: config.nodeEnv,\n  tracesSampleRate: 1.0\n});\n\napp.use(Sentry.Handlers.errorHandler());\n```\n\n---\n\n## Test Checklist\n\n- [ ] Health endpoint responds\n- [ ] Form webhook accepts data\n- [ ] Worker routing works\n- [ ] WhatsApp messages send (test phone)\n- [ ] Database saves submissions (if using MongoDB)\n- [ ] Worker response processing works\n- [ ] Error handling works\n- [ ] Logs are generated\n- [ ] Monitoring alerts work\n- [ ] No sensitive data in logs\n\n---\n\nYour app is now tested and ready for production! 🚀\n\n